========================================================================
AGENT 2 — SIGNAL & SCORING AUDIT REPORT
========================================================================
Date: 2026-03-06
Scope: process_bar() through trade signal generation
Mode: READ-ONLY

------------------------------------------------------------------------
1. HTF CHOKE-POINT GATE
------------------------------------------------------------------------

Finding 1.1: main.py HTF Gate — CORRECT
Location: main.py lines 582-617
The HTF directional gate is a single choke-point that ALL signal sources
pass through. It checks entry_direction against htf_bias.htf_allows_long /
htf_bias.htf_allows_short BEFORE the HC score gate (line 620).
Gate order: HTF gate (582) -> HC score gate (620) -> HC stop gate (670).

Fail-safe: When htf_bias is None, ALL trades are blocked (lines 609-617).

Finding 1.2: orchestrator.py HTF Gate — CORRECT
Location: orchestrator.py lines 385-410
Identical structure. HTF gate at 388-403, BEFORE HC score gate at 418.
Fail-safe when htf_bias is None blocks all trades (lines 404-410).

Finding 1.3: Signal Path Completeness — ALL PATHS PASS THROUGH GATE
Three signal paths exist:
  1. Sweep signals — set entry_direction, pass through HTF gate at 586
  2. UCL confirmed signals — set entry_direction, pass through same gate
  3. Aggregator signals — under PATH C, CANNOT independently trigger trades

No code path bypasses the HTF choke-point gate.

Severity: INFO — No issues. Gate is correctly positioned and covers all paths.

------------------------------------------------------------------------
2. HC SCORE GATE
------------------------------------------------------------------------

Finding 2.1: Threshold Value — CORRECT (exactly 0.75)
Location: config/constants.py line 17
  HIGH_CONVICTION_MIN_SCORE: float = 0.75

Finding 2.2: Comparison Operator — CORRECT
main.py line 620:
  if entry_direction is not None and entry_score < HIGH_CONVICTION_MIN_SCORE:
Uses < (reject if below) => scores >= 0.75 PASS. Correct.

orchestrator.py line 418:
  if entry_score < HIGH_CONVICTION_MIN_SCORE:
Same operator. Consistent.

signal_bridge.py line 152:
  if decision.signal_score < MIN_SIGNAL_SCORE:
Same operator. MIN_SIGNAL_SCORE aliased from HIGH_CONVICTION_MIN_SCORE.

All three locations consistent. >= 0.75 passes.

Severity: INFO — Threshold is exactly 0.75, comparison is correct.

------------------------------------------------------------------------
3. MAX STOP GATE
------------------------------------------------------------------------

Finding 3.1: Cap Value — CORRECT (exactly 30.0 points)
Location: config/constants.py line 18
  HIGH_CONVICTION_MAX_STOP_PTS: float = 30.0

Finding 3.2: Comparison Operator — CORRECT
main.py line 670:
  if raw_stop > HIGH_CONVICTION_MAX_STOP_PTS:
Uses > (reject if above) => stops <= 30.0 PASS. Correct.

orchestrator.py line 437:
  if raw_stop > HIGH_CONVICTION_MAX_STOP_PTS:
Same operator. Consistent.

signal_bridge.py line 195:
  if stop_distance > MAX_STOP_DISTANCE_PTS:
Same operator. Consistent.

Finding 3.3: Gate Ordering — CORRECT (after stop computation)
Stop computed at main.py line 654, then checked at line 670.
In orchestrator.py: computed at 430, checked at 437.

Finding 3.4: Post-Modifier Re-check — CORRECT
After institutional modifiers widen the stop (line 746), the gate is
re-checked at line 750. Prevents modifier bypass.

Severity: INFO — Cap is exactly 30.0, comparison correct, re-checked.

------------------------------------------------------------------------
4. SWEEP-ONLY TRIGGER (PATH C)
------------------------------------------------------------------------

Finding 4.1: Sweep Is The Only Entry Trigger — CORRECT
main.py lines 410-458: Only the "if has_sweep:" block sets entry_direction.
The "elif has_signal:" block logs "no trade (sweep required)" and does NOT
set entry_direction.

orchestrator.py lines 354-381: Identical. Only sweep block sets direction.
Comment at line 380: "Aggregator alone cannot trigger (PATH C)".

Finding 4.2: Context Boosts — CORRECT VALUES
config/constants.py:
  CONTEXT_AGGREGATOR_BOOST: float = 0.05
  CONTEXT_OB_BOOST: float = 0.05
  CONTEXT_FVG_BOOST: float = 0.05

Applied as additive score boosts in main.py (422-442) and orchestrator.py
(361-379).

Finding 4.3: No Non-Sweep Direct Trade Execution — VERIFIED
enter_trade() calls:
  - main.py:775 — only reachable after sweep sets entry_direction
  - orchestrator.py:483 — only reachable after sweep sets entry_direction
No non-sweep path reaches trade execution.

Finding 4.4: UCL as Second Entry Path — ACCEPTABLE
UCL confirmed signals can also set entry_direction. However, UCL watches
are only created from sweep signals with wide stops (line 672: entry_source
== "sweep"), so the original trigger was always a sweep.

Severity: INFO — PATH C correctly enforced.

------------------------------------------------------------------------
5. UCL V2 ROUTING
------------------------------------------------------------------------

Finding 5.1: Signals 0.60-0.74 — CORRECTLY REJECTED
SWEEP_MIN_SCORE = 0.70. Sweeps scoring 0.70-0.74 pass has_sweep but fail
HC gate at line 620 (entry_score < 0.75). No watch state creation for
sub-0.75 signals. Correct per UCL v2 design.

Finding 5.2: Signals >= 0.75 with Tight Stops — CORRECT
Proceed to trade execution at main.py line 775.

Finding 5.3: Wide-Stop Sweep Conversion — CORRECT
main.py lines 670-694: When sweep has score >= 0.75 but stop > 30pt and
UCL enabled, routed to _create_wide_stop_watch() (line 674). Creates
WatchState with setup_type="wide_stop_sweep" and confirmation conditions
["RECLAIM", "FVG_FORM", "FVG_TAP"].

Finding 5.4: Watch State Expiry — CORRECT
signals/watch_state.py lines 164-171: bars_elapsed incremented each bar,
checked against expiry_bars. Expired watches removed. Default 90 bars
for wide-stop watches, 60 bars for generic.

Finding 5.5: Watch State Invalidation — CORRECT
LONG watches invalidated when bar.close < invalidation_price.
SHORT watches invalidated when bar.close > invalidation_price.

Severity: INFO — UCL v2 routing correctly implemented.

------------------------------------------------------------------------
6. SESSION VOLATILITY SCALER
------------------------------------------------------------------------

Finding 6.1: Feature Flag Default — CORRECT (OFF)
File: features/session_volatility.py
  SESSION_VOLATILITY_SCALING_ENABLED = os.getenv("SESSION_VOLATILITY_SCALING", "false").lower() == "true"
Default is "false" => False.

Finding 6.2: When OFF, Returns Raw ATR — CORRECT
get_scale_factor() returns 1.0, scale_atr() returns atr_value * 1.0.

Finding 6.3: Usage in main.py — CORRECT
main.py line 642: scaled_atr = self._session_scaler.scale_atr(...)
When disabled, raw ATR passes through unchanged.

Severity: INFO — Feature flag defaults OFF, raw ATR unmodified.

------------------------------------------------------------------------
7. QUANTDATA CONTEXT SCORING
------------------------------------------------------------------------

Finding 7.1: Scoring DISABLED — CORRECT
File: data_feeds/context_scoring.py line 32
  ENABLED = False
Hard-coded False. score_adjustment() returns 0.0 when disabled.

Finding 7.2: Not Imported by Signal/Gate Modules — VERIFIED
ContextScorer not called from main.py, orchestrator.py, or signal_bridge.py.

Finding 7.3: Even If Enabled, Cannot Override Gates — VERIFIED
No code path wires ContextScorer output into entry_score or gate logic.

Finding 7.4: QuantData Is LOG-ONLY in Orchestrator — CORRECT
orchestrator.py lines 549-557: _market_context used only in action_result
dict for logging metadata. Never touches entry_score or gates.

Severity: INFO — Context scoring completely inert.

------------------------------------------------------------------------
8. ADDITIONAL FINDINGS
------------------------------------------------------------------------

Finding 8.1 [INFO]: NaN Guards — Present and Correct
  - main.py lines 527-532: NaN guard on entry_score
  - main.py lines 662-667: NaN guard on raw_stop
  - orchestrator.py lines 412-415: NaN guard on entry_score
  - orchestrator.py lines 432-433: NaN guard on raw_stop
  - signal_bridge.py lines 137-142: NaN guards on score, ATR, entry_price

Finding 8.2 [LOW]: Reject-Style Comparisons — Cosmetic
Uses "entry_score < 0.75" (reject) rather than "entry_score >= 0.75" (allow).
Semantics identical but slightly harder to audit at a glance.

Finding 8.3 [INFO]: Signal Bridge Belt-and-Suspenders
signal_bridge.py lines 144-200: Re-validates score, HTF direction, and
stop distance as a secondary safety layer. Good defense-in-depth.

========================================================================
SUMMARY
========================================================================

| Severity | Count | Key Finding                                    |
|----------|-------|------------------------------------------------|
| CRITICAL | 0     | None                                           |
| HIGH     | 0     | None                                           |
| MEDIUM   | 0     | None                                           |
| LOW      | 1     | Reject-style comparison readability (cosmetic)  |
| INFO     | 12    | All gates verified correct, defense-in-depth    |

OVERALL SIGNAL & SCORING VERDICT: PASS

All seven audit tasks verified clean. HTF gate covers all paths, HC score
threshold is exactly 0.75 with correct >= semantics, max stop cap is
exactly 30.0 with correct <= semantics, PATH C sweep-only trigger
enforced, UCL v2 routing correct, session volatility OFF, QuantData inert.
