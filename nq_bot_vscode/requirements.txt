# NQ Trading Bot — Pinned Dependencies
# =====================================
# Financial systems pin exact versions for reproducibility.
# Update only after testing across the full 6-month OOS window.
#
# Install: pip install -r requirements.txt

# ── Core (computation) ────────────────────────────────────────
numpy==2.4.2

# ── Networking (IBKR + Tradovate connectors) ──────────────────
aiohttp==3.13.3

# ── Database ──────────────────────────────────────────────────
asyncpg==0.30.0

# ── Dashboard (web server) ────────────────────────────────────
fastapi==0.115.0
uvicorn==0.34.0

# ── ML (entry classifier) ──────────────────────────────────────
lightgbm>=4.0.0
scikit-learn>=1.3.0

# ── Dev / Testing ─────────────────────────────────────────────
pytest==9.0.2
pytest-asyncio==1.3.0
